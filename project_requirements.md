# ESP32-S3 KVM 控制器当前需求

更新：2026-09-12。本文件是当前产品需求基线，替代项目根目录此前的阶段性需求描述。

## 1. 产品目标

控制器服务于 Mac 和 Windows 两台电脑，统一完成：

1. 展示两台电脑的 CPU 温度、内存占用和在线状态。
2. 联动切换 KOORUI X41Q 显示器输入和 USB 共享器。
3. 控制 Mac 外置散热器，并提供标准、静音、性能三种温控模式。
4. 使用三个实体机械按键完成主要操作。
5. 使用 2.4 英寸 ILI9341 非触摸屏显示状态和多页信息。

ESP32在屏幕关闭时仍保持运行；本项目不控制显示器市电。

## 2. 已确认硬件

- ESP32-S3-N16R8。
- 2.4 英寸、240×320、ILI9341、SPI、非触摸 LCD；最终为 320×240 横屏。
- 三个两脚常开机械键盘轴体。
- 两块 YS-HK1 单路机械继电器模块，5V供电、高电平触发。
- USB共享器控制接口使用Micro-USB转接板。
- 散热器控制接口使用专用Type-C转接板，并将Type-C的VBUS与GND分别复用为两根无源按钮触点线；该接口不是标准充电或数据口，VBUS不得接入5V，GND也不得与ESP32系统地相连。
- KOORUI X41Q输入值：DP=`7`、HDMI1=`5`、HDMI2=`6`。
- X41Q DDC写入有效，但读回不可靠，不能用读回值作为唯一确认依据。

## 3. 固定GPIO基线

| 功能 | GPIO |
| --- | ---: |
| 左侧按钮 | 4 |
| 中间按钮 | 5 |
| 右侧按钮 | 6 |
| USB共享器继电器T1 | 7 |
| 散热器继电器T1 | 8 |
| LCD DC | 9 |
| LCD CS | 10 |
| LCD MOSI | 11 |
| LCD SCLK | 12 |
| LCD MISO | 13 |
| LCD RST | 14 |
| LCD背光开关 | 15 |

按钮启用内部上拉，未按为HIGH、按下接GND为LOW。GPIO7和GPIO8必须在上电、复位、烧录和异常状态下默认LOW；每个T1使用10kΩ下拉到GND。

两块继电器的VCC使用可靠5V供电，不从ESP32的3.3V轨供电。YS-HK1的`GND`为触发参考地，`RGND`为继电器电源负极。负载侧只使用COM和NO，NC不接。

LCD BLK接地电流实测约0.2mA，连接GPIO15并使用开漏模式：LOW关闭背光，高阻释放开启；不得推挽输出HIGH。

## 4. 按钮功能

### 左侧按钮

- 单击：在Mac与Windows之间执行显示器和USB联动切换。
- 双击：交换ESP32保存的显示器目标和USB推定归属，用于二态校准；不发送DDC、不触发继电器。
- 长按：开关外接显示器与LCD显示；ESP32、网络和温控任务继续运行。

单击切换前必须确认目标电脑和负责发送DDC的当前电脑在线。DDC失败时不得切USB。DDC写入成功后等待显示器换源，再给USB继电器一个脉冲。一次流程完成后进入至少5秒冷却；冷却期间忽略新请求，不排队。

### 中间按钮

- 单击：在已经注册的LVGL页面之间循环翻页。
- 双击：切换上一页；长按：当前未分配功能。
- 翻页只改变本地显示，不修改KVM、网络和温控状态。

### 右侧按钮

- 完整语义以`esp32/latest/docs/cooler_control_spec.md`为准。
- Manual模式下单击升档、双击降档；三个自动模式下单击/双击调整目标温度。
- 按住500–999ms后松开切换下一模式；持续到1000ms则立即进入散热器关机动作，松开时不再切换模式。

## 5. KVM联动

ESP32作为WebSocket服务器，Mac和Windows作为客户端。切换请求由当前活跃输入对应的电脑执行DDC，然后ESP32控制USB共享器继电器。

```text
左键单击
→ 检查当前主机与目标主机在线
→ 向当前主机发送唯一request_id的DDC命令
→ DDC写入失败：停止
→ DDC写入成功：等待换源
→ USB继电器单次脉冲
→ 更新显示器目标和USB推定归属
→ 进入5秒冷却
```

状态必须区分“命令已写入”“读回确认”“推定状态”和“失败”。重试不得重复产生继电器脉冲。

### 当前开发版显示器待机与唤醒

- X41Q已实测确认：只要任一输入仍有有效视频信号，就可能自动切换过去；所有输入停止后才进入闪灯待机。
- 进入待机时由ESP32命令Mac和Windows同时停止显示输出，电脑本身和客户端继续运行。
- 唤醒时只恢复用户选择的目标主机输出，避免两台信号同时恢复造成自动选源错误。
- 任一客户端离线或未确认停止输出时不得报告待机成功。
- 该流程绑定ESP32左键长按背光开关：关背光时请求双主机停止输出，再次长按时恢复背光并自动唤醒关屏前的当前主机。
- ESP32背光关闭期间注销全部普通按键动作，只允许左键长按唤醒；所有其他事件必须立即丢弃。
- 详细流程、协议边界和验收标准见`esp32/latest/docs/display_sleep_plan.md`。

## 6. 散热器控制

散热器继电器并联原按钮触点，通过闭合时序模拟单击、双击和长按。完整模式、第三按钮语义、上电校准、继电器基准时序和自动控温算法统一见`esp32/latest/docs/cooler_control_spec.md`。

模式固定为 Manual、Standard、Silent、Turbo。Standard、Silent、Turbo 的默认目标温度分别为 `40°C`、`50°C`、`28°C`，目标值可调，不得作为不可修改的算法常量。

散热器只有离散档位，正式控制采用：

- 温度平滑；
- 分档目标；
- 升降档回差；
- 最短档位保持时间；
- 过热保护；
- 推定档位和人工校准。

Mac离线、温度无效、数据过期或档位未知时暂停自动调档。

## 7. 软件与协议

- ESP-IDF保持v6.1，不在重构期间升级工具链。
- 新版UI使用LVGL；原测试UI不继续扩展。
- protocol v1字段和限制保持与`StatsForKVM/PROTOCOL.md`一致。
- Wi-Fi凭据只保存在被忽略的本地配置，不写入源码、文档、归档或可分发二进制。
- UI、网络、按钮和继电器必须解耦；只有UI任务可以调用LVGL。
- UI故障或页面切换不能让继电器保持吸合。

## 8. UI方向

- 主风格固定为System 7，辅以少量System 1黑白像素元素；不混用Mac OS 8/9 Platinum控件。
- 所有屏幕文本使用纯英文，不加载中文字库。
- 顶部固定保留System 7风格菜单栏，显示页面名、Internet、Mac和Windows连接状态。
- 长按左键关闭LCD时必须物理关闭背光，使屏幕完全不发光；只绘制黑色画面不算关闭。
- 320×240横屏、非触摸、三键操作。
- 使用合法可分发的开源字体和自制图标，不直接打包Apple专有字体或系统资源。
- 当前页面：SYSTEM、WEATHER、MARKETS；页面内容和顶部状态栏已冻结于`esp32/latest/docs/ui_spec.md`。

## 9. 首版验收

- 冷启动和复位时两路继电器无误动作。
- 三键单击、双击和长按按定义工作，无重复触发。
- LVGL连续运行无花屏、卡死和明显内存下降。
- Mac和Windows数据在线、离线、过期和恢复显示正确。
- 双向KVM联动不少于100次，失败时USB不误切。
- 网络、ESP32或电脑重启后能够恢复，并明确显示推定/未知状态。
- 三种散热模式和异常暂停策略通过实机验证。
- 完整系统连续运行至少24小时。
